# 对话记录

## 2026-09-19：面包屑目录页沿用平台文档侧栏

- 用户要求：此前给 SDK、UIKit、CallKit 面包屑目录页增加的专用导航不符合预期；应显示来源平台本身的文档侧栏，例如 Android V5 的“入门指引、迁移指南、使用 MCP 集成”等。
- 处理：将目录页默认侧栏换成真实平台菜单，并在面包屑链接中携带来源平台上下文；侧栏、平台选择器与版本选择器在目录页保持一致。无来源的直接访问按分类展示默认平台菜单。
- 修改：`docs/.vuepress/components/breadcrumbSidebarContext.ts`、`BreadCrumb.vue`、`Sidebar.vue`、`PlatformSwitch.vue`、`UIKitSwitch.vue`、`CallKitSwitch.vue`、`docs/.vuepress/sidebar/index.ts`。
- 验证：开发预览中从 Android V5 点击 SDK、从 iOS V4 点击 V4、从聊天室 UIKit iOS 点击聊天室 UIKit、从 CallKit iOS 点击 V4，均显示对应平台原有菜单；直接打开 SDK V4 显示 Android V4 菜单。目录页相关分组仍默认收起。
- 待确认：无。

## 2026-09-20：面包屑补齐多级侧栏菜单

- 用户要求：产品介绍、UIKit、CallKit、SDK、服务端和增值服务的文档面包屑应包含侧栏中的全部上级菜单；每一级菜单均可点击，并打开只列出其直属子菜单的目录页。
- 处理：基于当前页面实际生效的侧栏配置自动查找完整菜单链，生成可点击的多级面包屑；新增通用菜单目录页，通过侧栏来源和菜单索引动态列出直属子菜单，并保持来源平台原有侧栏、平台选择器与版本选择器。
- 示例验证：内容审核“计费说明”列出国内/海外计费说明；Android FCM 路径包含“离线推送 > 集成第三方推送”，两级目录页分别只列直属子项；另抽查产品介绍、UIKit、CallKit 和服务端多级菜单。
- 修改：`docs/.vuepress/components/sidebarBreadcrumb.ts`、`useSidebarBreadcrumb.ts`、`SidebarMenuPage.vue`、`BreadCrumb.vue`、`Sidebar.vue`、`PageTitle.vue`、各平台切换组件、`docs/.vuepress/client.ts`、`docs/.vuepress/sidebar/index.ts`、`docs/breadcrumb/menu.md`。
- 验证：`git diff --check` 通过；VuePress 完整构建成功，共生成 1652 个页面。构建仅输出项目原有的 CSS 与 Element Plus 警告。
- 待确认：无。

## 2026-09-19：简化 UIKit / CallKit 面包屑与恢复侧栏样式

- 用户要求：CallKit、单群聊 UIKit 的面包屑不再出现版本；点击两类总览入口直接展示各平台并链接入门指引。修复所有文档侧栏菜单、子菜单和标题前意外出现的项目符号。
- 处理：仅从这两类面包屑路径移除版本层级，保留现有面包屑入口与来源平台侧栏；将两份总览内容整理为平台入口；为自定义侧栏列表组件补回主题原有的列表样式。
- 修改：`docs/.vuepress/components/BreadCrumb.vue`、`docs/.vuepress/components/SidebarLinks.ts`、`docs/callkit/README.md`、`docs/uikit/chatuikit/README.md`。
- 验证：本地预览核对 CallKit iOS、单群聊 UIKit Flutter 的面包屑及总览链接；截图核对 CallKit 入门指引与 Android 登录文档侧栏的菜单、展开子菜单和标题不再带项目符号；所有新增平台入门指引目标文件存在，`git diff --check` 通过。
- 待确认：无。

## 2026-09-20：UIKit 总览移除版本入口

- 用户要求：点击面包屑中的“UIKit”后，页面只显示“单群聊 UIKit”和“聊天室 UIKit”，不再列出单群聊 UIKit 的 V4/V2/V1 版本。
- 处理：从 `docs/uikit/README.md` 删除三个版本子链接，保留两个 UIKit 分类链接；目标分类页面均存在。
- 验证：两个链接目标文件存在，`git diff --check` 通过。
- 待确认：无。

## 2026-09-20：服务端回调面包屑去重

- 用户要求：将服务端回调文档面包屑中的第一个固定“回调”移除，保留第二个侧边栏菜单层及其目录页，并将路径整理为“首页 > 服务端 > 回调功能 > 回调事件 > 消息回调 > 文档一级标题”。
- 处理：回调文档不再额外插入固定分类层；保留真实侧边栏“回调”分组的链接，并在面包屑和对应目录页中统一显示为“回调功能”。服务端左侧栏结构和名称保持不变。
- 修改：`docs/.vuepress/components/BreadCrumb.vue`、`SidebarMenuPage.vue`、`sidebarBreadcrumb.ts`、`useSidebarBreadcrumb.ts`。
- 验证：`git diff --check` 通过；两份 Vue 组件通过 `@vue/compiler-sfc` 解析与脚本编译。VuePress 全量构建已完成数据初始化，但 Vite 编译阶段因 esbuild 内存不足退出，未出现与本次修改相关的语法或类型错误；8081 端口为未加载本轮组件的旧预览实例，未作为最终验证依据。
- 待确认：无。

## 2026-09-20：各类入口页补齐侧栏一级菜单

- 用户要求：服务端回调面包屑将“回调功能”改为“回调”；SDK V4/V5、UIKit、聊天室 UIKit 和 CallKit 的平台入口页补齐真实侧栏中的全部一级菜单及链接；UIKit、CallKit、服务端入口页补齐侧栏导航；服务端排除“已废弃内容”；产品简介入口页与左侧栏对齐并排除“帮助中心”和“已废弃内容”；修复产品简介入口页自动展开“购买指南”。
- 处理：新增复用真实侧栏配置的 `SidebarOverviewPage` 组件，一级分组统一链接到第一个子文档；27 个 SDK/UIKit/CallKit 平台入口页改为动态生成完整一级菜单，UIKit 与 CallKit 总入口在保留原分类/平台入口的基础上增加当前来源平台的文档导航；服务端和产品简介入口页使用相同机制并按要求过滤菜单。将产品入口页纳入侧栏分组默认收起判定。回调面包屑保留的侧栏分组改回显示“回调”。
- 示例核对：V4 Android“常见问题”链接 `/v4/android/group_@.html`；V4 小程序“其他帮助”链接 `/v4/applet/uniappnativeapp.html`；服务端菜单包含“特性限制”且不包含“已废弃内容”。多级菜单继续使用通用面包屑目录页列出直属子菜单。
- 修改：`docs/.vuepress/components/SidebarOverviewPage.vue`、`SidebarLinks.ts`、`sidebarBreadcrumb.ts`、`docs/.vuepress/client.ts`，以及产品简介、服务端、UIKit、CallKit 和各 SDK/平台入口 `README.md`。
- 验证：27 个目标平台入口页覆盖检查通过；相关 Vue 组件通过 `@vue/compiler-sfc` 脚本与模板编译；通过 Vite 实际加载侧栏配置核对一级菜单及首个子文档链接；`git diff --check` 通过。
- 待确认：无。

## 2026-09-20：面包屑目录页统一按菜单层级跳转

- 用户要求：SDK、UIKit、CallKit、服务端、产品简介和增值服务的面包屑入口/目录页中，有子菜单的列表项应进入对应的面包屑目录页，只有无子菜单的叶子项才直接打开具体文档；例如 V4 小程序“其他帮助”进入子菜单列表，“入门指引”仍打开 `beginner_guide.html`。
- 处理：新增统一的 `sidebarEntryLink` 链接选择规则，并让平台/分类总览组件与多级菜单目录组件共同使用；保留原始侧栏索引，确保过滤部分入口菜单后分组链接仍正确。SDK 版本页、UIKit/聊天室 UIKit 平台页、CallKit 平台页的入口改为各自目录页。为即时推送、内容审核、消息翻译、搜索服务端消息和语音转文字新增/补齐服务目录页，增值服务总览先进入具体服务目录。
- 示例验证：V4 小程序“其他帮助”生成 `/breadcrumb/menu.html?...group=17...`，目录页直属子项为“Uniapp 生成原生 Android/iOS 应用、小程序模板使用指南、如何配置服务器域名、Vue3 项目在 H5 平台发布的注意事项”；“入门指引”仍为 `/v4/applet/beginner_guide.html`。另验证服务端“消息管理”、UIKit“特性”、CallKit“产品介绍”、即时推送“计费说明”均生成目录页链接。
- 修改：`docs/.vuepress/components/sidebarBreadcrumb.ts`、`SidebarOverviewPage.vue`、`SidebarMenuPage.vue`、`BreadCrumb.vue`、`SidebarLinks.ts`；SDK/UIKit/CallKit/增值服务分类入口文档；五类增值服务的 `README.md` 目录页。
- 验证：3 个相关 Vue 组件通过脚本与模板编译；32 个目录入口目标存在；分类入口页不再直接绕过目录页；`git diff --check` 通过。
- 待确认：无。

## 2026-09-20：UIKit、CallKit 总览简化及增值服务侧栏上下文

- 用户要求：UIKit 总览只保留“单群聊 UIKit”和“聊天室 UIKit”；CallKit 总览只保留 Android、iOS、Web；从即时推送、内容审核等服务页面点击面包屑“增值服务”时，页面左侧栏应切换为当前服务的完整侧栏。
- 处理：移除 UIKit/CallKit 总览页追加的“文档导航”区块；在增值服务侧栏上下文中加入五个服务根路径，并让面包屑根据当前服务携带 `sidebar` 参数。增值服务总览页根据该参数解析对应服务侧栏，直接访问时默认使用即时推送侧栏。
- 验证：五个服务文档路径均能解析到对应侧栏根；从内容审核返回增值服务解析为 `/value-added/moderation`；直接访问增值服务总览默认解析为 `/value-added/push`；UIKit/CallKit 总览页仅保留要求的列表项；相关 Vue 组件编译和 `git diff --check` 通过。
- 修改：`docs/.vuepress/components/breadcrumbSidebarContext.ts`、`BreadCrumb.vue`、UIKit/CallKit 总览 `README.md`。
- 待确认：无。

## 2026-09-20：修复增值服务总览页缺少左侧栏

- 用户反馈：从即时推送、内容审核等文档点击面包屑“增值服务”后，页面没有显示当前服务的左侧栏。
- 根因：`/value-added/` 没有注册全局侧栏，主题在该路由上判定为无侧栏；仅通过面包屑 query 传递服务根路径无法挂载侧栏。
- 处理：在 `docs/.vuepress/sidebar/index.ts` 注册 `/value-added/` 默认侧栏，并为默认即时推送菜单补全 `/value-added/push/` 链接前缀；`Sidebar.vue` 继续依据面包屑的 `sidebar` 参数切换为即时推送、内容审核、消息翻译、搜索服务端消息或语音转文字的完整侧栏。
- 验证：本地预览中从即时推送和内容审核页面点击“增值服务”，分别显示对应服务的完整一级菜单；内容审核侧栏包含“计费说明、开通审核服务、审核规则配置、审核机制与处理、数据查询与通知”，即时推送侧栏包含“服务介绍、计费说明、快速入门、Android 接入、iOS 接入、服务端 REST”。相关 Vue 组件编译通过，`git diff --check` 通过。
- 待确认：无。

## 2026-09-20：增值服务总览增加实时音视频入口

- 用户要求：在点击面包屑“增值服务”后显示的服务列表中补充“实时音视频”。
- 处理：新增“实时音视频”列表项，链接到 `/callkit/` CallKit 总览页，由开发者继续选择 Android、iOS 或 Web，避免直接落到单一平台文档。
- 验证：`/callkit/` 总览页存在并列出 Android、iOS、Web 三个平台；`git diff --check` 通过。

## 2026-09-20：对齐增值服务总览列表顺序

- 用户要求：将面包屑“增值服务”页面中的列表顺序与导航栏一致。
- 处理：调整为“内容审核、实时音视频、即时推送、消息翻译、搜索服务端消息、语音转文字”，各项链接保持不变。
- 验证：已与 `docs/.vuepress/navbar/index.ts` 中的增值服务菜单顺序核对，`git diff --check` 通过。

## 2026-09-21：重构服务端面包屑入口层级

- 用户要求：服务端总览只显示“REST API、回调、服务端 SDK”；REST API 页面按服务端侧栏列出指定菜单；服务端 SDK 下增加“SDK 介绍”和“API 参考”两级目录，并补齐对应文档链接。
- 处理：新增 REST API、回调、服务端 SDK、SDK 介绍和 API 参考目录页；扩展 `SidebarOverviewPage` 的 `include` 筛选，使 REST API 页面复用真实服务端侧栏并按子菜单自动生成面包屑目录链接；服务端 SDK 文档侧栏统一命名为“服务端 SDK”，API 参考移除未要求的 PHP 条目；补充这些目录页和 Server SDK 文档的面包屑层级。
- 验证：5 个新增目录文件存在，相关 Vue 组件通过 `@vue/compiler-sfc` 编译，`git diff --check` 通过。

- 补充修正：REST API 面包屑统一链接到新的 REST API 目录页；服务端 SDK 文档补齐“服务端 SDK > SDK 介绍/API 参考”路径；回调事件目录页保留“服务端 > 回调”父级，避免跳回原始侧栏通用目录页。
- 预览核对：服务端入口、REST API、服务端 SDK、SDK 介绍、API 参考及回调事件目录页均可正常打开，列表链接和面包屑层级符合要求。
- 另外修复：服务端的通用子菜单目录页不再显示 SDK 平台切换器，继续使用服务端侧栏。

## 2026-09-21：统一维护 Android 更新日志

- 用户要求：停止维护 `docs/v4/android/releasenote.md` 中的重复更新日志，仅维护 `docs/document/android/releasenote.md`。
- 处理：清空 V4 Android 更新日志原有内容，仅保留指向 `/document/android/releasenote.html` 的“查看 Android IM SDK 更新日志”链接。
- 修改：`docs/v4/android/releasenote.md`。
- 验证：目标文档存在，链接地址正确，`git diff --check` 通过。

## 2026-09-21：比较 iOS 与 Web 的 V5/V4 更新日志

- 用户要求：分别比较 `docs/document` 与 `docs/v4` 下 iOS、Web 的更新日志，暂时忽略链接差异，仅列内容差别。
- iOS 结论：`docs/document/ios/releasenote.md` 独有 `v5.0.0 Dev 2026-8-12` 和 `v4.24.1 Dev 2026-9-2`；从 `v4.24.0` 起的其余共同历史内容一致。
- Web 结论：`docs/document/web/releasenote.md` 独有 `v5.1.0 Dev 2026-8-21` 和 `v5.0.0 Dev 2026-8-12`；从 `v4.24.1` 起的其余共同历史内容一致。
- 处理范围：仅分析原文，未修改四份更新日志。

## 2026-09-21：统一 V4 版本更新日志的平台文档链接

- 用户要求：`docs/document` 下 Android、iOS 和 Web 更新日志中，v4.x 版本涉及的平台文档链接统一指向 `docs/v4` 下对应平台的同名文档。
- 处理：仅改写 v4.x 版本块中的平台文档链接，v5.x、服务端、增值服务和控制台等链接保持原路径；原相对链接和 `/document/{platform}/` 链接分别改为 `/v4/android/`、`/v4/ios/` 和 `/v4/web/`。EaseCallKit 因不属于 `docs/v4` SDK 目录，改为对应 `/callkit/{platform}/easecallkit.html` 链接。
- 同步修正：iOS `message_send_.html` 拼写错误，以及若干已变更的目标锚点，包括消息 ID 查询、Crash 库冲突、动态加载 `.so`、好友列表、会话列表、群成员和群组详情等链接。
- 修改：`docs/document/android/releasenote.md`、`docs/document/ios/releasenote.md`、`docs/document/web/releasenote.md`。
- 验证：v4.x 版本块中无遗留的平台相对链接或 `/document/{platform}/` 链接；新增的 V4 目标文件全部存在，所有非重定向页锚点均能匹配，`git diff --check` 通过。

## 2026-09-22：同步 Android 4.25.1 会话文档

- 用户要求：参考 easemob/easemob-doc PR #1815 中的 iOS 文档改动，结合 Android 4.25.1 更新日志和 Android SDK 代码，同步修改 Android 文档，并判断数据同步 WebSocket 接口移除及默认服务域名切换是否影响 `private_ip_domain.md`。
- 核对结论：Android 4.25.1 的 `EMOptions#setEnableChatroomConversation(boolean)` 默认值为 `false`；本地会话分页接口为 `EMChatManager#asyncGetConversationsFromDB(String, int, EMValueCallBack)`，调用前需设置 `setAutoLoadAllConversations(false)`，每页数量范围为 `[1,100]`。
- 已修改：`docs/document/android/conversation_list.md`、`conversation_overview.md`、`initialization.md`、`message_send.md`、`message_receive.md`，补充聊天室会话创建策略、本地会话分页说明及示例。
- `private_ip_domain.md` 判断：无需修改。该文档中的 `setWebSocketServer/setWebSocketPort` 配置 IM 长连接 WebSocket，并非已移除的 `setSyncDataWebSocketServer/setSyncDataWebSocketPort`；`setUseAgoraChatDomain` 用于切换公有云默认服务域名，不属于私有云地址配置。
- 保留内容：未改动现有 Android/iOS release notes 的未提交修改。
- 验证：目标 API 名称、默认值、分页范围和错误码均与 Android SDK 源码一致；`git diff --check` 通过。
- 待确认：无。
