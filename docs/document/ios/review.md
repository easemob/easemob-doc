# 文档审计待处理项

审计范围：`doc` 目录中以字母开头的 63 份 Markdown 文档（包含 `push` 下 11 份）。

## 1. 群详情链接错误

- 文件：[group_members.md](group_members.md:205)
- 现状：链接目标为 `group_attribute.html#获取群组详情`。
- 问题：当前文档文件名为 `group_attributes.md`，目标链接少了 `s`。
- 建议：改为 `group_attributes.html#获取群组详情`。
